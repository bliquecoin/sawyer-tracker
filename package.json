{
  "name": "sawyer-tracker",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "check": "node --check app.js && node --check lib/tracker-core.js && node -e \"JSON.parse(require('fs').readFileSync('manifest.webmanifest', 'utf8'))\"",
    "test": "node --test tests/*.test.js",
    "test:e2e": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "1.61.1",
    "fflate": "^0.8.2"
  }
}
